part of '../onboarding_page.dart';

class BuildGetStartedButton extends StatelessWidget {
  const BuildGetStartedButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Positioned(child: Center());
  }
}