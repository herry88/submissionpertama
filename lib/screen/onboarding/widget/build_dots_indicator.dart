part of '../onboarding_page.dart';

class BuildDotsIndicator extends StatelessWidget {
  const BuildDotsIndicator({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Positioned(
      child: Center(),
    );
  }
}
